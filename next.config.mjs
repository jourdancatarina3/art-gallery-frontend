/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img.freepik.com', 'drive.google.com', 'i.pinimg.com', 'res.cloudinary.com'],
  },
};

export default nextConfig;
